import 'package:flutter/material.dart';

class BuilderCompleteProfile extends StatelessWidget {
  const BuilderCompleteProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
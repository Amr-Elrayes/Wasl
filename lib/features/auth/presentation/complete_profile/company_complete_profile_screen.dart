import 'package:flutter/material.dart';

class CompanyCompleteProfile extends StatelessWidget {
  const CompanyCompleteProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}